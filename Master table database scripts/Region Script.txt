CREATE TABLE Region 
(
    UID	int not null auto_increment unique,
    REGIONKEY VARCHAR(20) not null,
    REGIONVALUE	VARCHAR(20) not null,
    primary key(REGIONKEY)
);

desc Region;

INSERT INTO Region (UID, REGIONKEY, REGIONVALUE) VALUES
	('1', 'NORTH', 'North'),
	('2', 'SOUTH', 'South'),
	('3', 'EAST', 'East'),
	('4', 'WEST', 'West');
    
select * from Region;