CREATE TABLE LOB 
(
    UID	int not null auto_increment unique,
    LOBKEY VARCHAR(20) not null,
    LOBVALUE VARCHAR(20) not null,
    primary key(LOBKEY)
);

desc LOB;

INSERT INTO LOB (UID, LOBKEY, LOBVALUE) VALUES ('1', 'ERS', 'ERS');

select * from LOB;

